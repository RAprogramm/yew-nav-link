name: Main CI

on:
  push:
    branches: ["main"]
    tags:
      - "v*"
  pull_request:
    branches: ["main", "develop"]
    types: [opened, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTFLAGS: -D warnings
  CARGO_REGISTRIES_CRATES_IO_PROTOCOL: sparse

jobs:
  # =====================================================
  # STAGE 1: FORMAT (run first)
  # =====================================================

  pr-analysis:
    name: PR Analysis
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    outputs:
      size: ${{ steps.pr-size.outputs.size }}
      weighted-score: ${{ steps.pr-size.outputs.weighted-score }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Fetch base branch
        run: git fetch origin ${{ github.event.pull_request.base.ref }} || git fetch origin main || true

      - name: Analyze PR Size
        id: pr-size
        uses: RAprogramm/rust-prod-diff-checker@v1
        with:
          config_file: .rust-diff-analyzer.toml
          max_prod_units: 100
          max_weighted_score: 500
          max_prod_lines: 500
          fail_on_exceed: false
          post_comment: false

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: nightly
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: false

      - name: Check formatting
        run: cargo +nightly fmt --all -- --check

  # =====================================================
  # STAGE 2: CLIPPY (runs after fmt)
  # =====================================================

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    needs: [fmt]
    if: needs.fmt.result == 'success'
    permissions:
      contents: read
      security-events: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: false

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings -W clippy::pedantic -W clippy::nursery

  # =====================================================
  # STAGE 3: TEST (runs after clippy)
  # =====================================================

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [clippy]
    if: needs.clippy.result == 'success'
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - uses: taiki-e/install-action@nextest

      - name: Run tests
        run: cargo nextest run --workspace --no-fail-fast --profile ci --status-level all --failure-output immediate --retries 1

      - name: Run doc tests
        run: cargo test --doc --workspace --all-features

  # =====================================================
  # STAGE 4: COVERAGE (runs after test)
  # =====================================================

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: [test]
    if: needs.test.result == 'success'
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: false

      - uses: taiki-e/install-action@cargo-llvm-cov

      - name: Generate coverage
        run: cargo llvm-cov --workspace --all-features --lcov --output-path lcov.info

      - name: Upload to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov.info
          flags: unittests,integration
          name: codecov-rust
          fail_ci_if_error: false
          verbose: true

  # =====================================================
  # STAGE 5: PACKAGE (runs after test)
  # =====================================================

  package:
    name: Package
    runs-on: ubuntu-latest
    needs: [test]
    if: needs.test.result == 'success'
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
      - uses: ./.github/actions/check-lockfile
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Check package metadata
        run: cargo publish --dry-run --allow-dirty

      - name: Package
        run: cargo package --locked

      - name: Verify package contents
        run: cargo package --locked --list

  # =====================================================
  # STAGE 6: AUDIT (runs after test)
  # =====================================================

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: [test]
    if: needs.test.result == 'success'
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-rust
        with:
          toolchain: stable
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: false

      - uses: taiki-e/install-action@cargo-audit

      - name: Run security audit
        run: cargo audit --deny yanked --ignore RUSTSEC-2025-0141 --ignore RUSTSEC-2024-0370

      - uses: taiki-e/install-action@cargo-deny

      - name: Run license audit
        run: cargo deny check

  # =====================================================
  # STAGE 7: QUALITY (runs after test)
  # =====================================================

  quality:
    name: Quality
    runs-on: ubuntu-latest
    needs: [test]
    if: needs.test.result == 'success'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v6

      - name: Run cargo-quality
        uses: RAprogramm/cargo-quality@v0
        with:
          path: src/
          fail_on_issues: ${{ github.ref == 'refs/heads/main' }}
          post_comment: ${{ github.event_name == 'pull_request' }}

      - name: Check for TODO comments
        run: |
          TODO_COUNT=$(grep -r "// TODO" src/ --include="*.rs" | wc -l)
          echo "TODO comments found: $TODO_COUNT"

  # =====================================================
  # STAGE 8: DEPLOY (main branch only, runs after package)
  # =====================================================

  supply-chain:
    name: Supply Chain
    needs: [package]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/supply-chain-security.yml
    permissions:
      contents: read
      id-token: write
      attestations: write
      packages: read
      security-events: write

  docs:
    name: Documentation
    needs: [package]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/docs.yml
    permissions:
      contents: read

  pages:
    name: Pages
    needs: [package]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/pages.yml
    permissions:
      contents: read
      pages: write
      id-token: write

  # =====================================================
  # STAGE 9: SUMMARY
  # =====================================================

  summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [pr-analysis, fmt, clippy, test, coverage, package, audit, quality]
    if: always()
    steps:
      - name: Generate CI Summary
        run: |
          echo "# CI Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| fmt | ${{ needs.fmt.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| clippy | ${{ needs.clippy.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| test | ${{ needs.test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| coverage | ${{ needs.coverage.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| package | ${{ needs.package.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| audit | ${{ needs.audit.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| quality | ${{ needs.quality.result }} |" >> $GITHUB_STEP_SUMMARY

      - name: Post failure comment
        if: failure() && github.event_name == 'pull_request'
        run: |
          BODY="## CI Failed\n\nThe following jobs failed:"
          if [ "${{ needs.fmt.result }}" == "failure" ]; then BODY="${BODY}\n- **Format**: Run \`cargo +nightly fmt --all\`"; fi
          if [ "${{ needs.clippy.result }}" == "failure" ]; then BODY="${BODY}\n- **Clippy**: Fix warnings"; fi
          if [ "${{ needs.test.result }}" == "failure" ]; then BODY="${BODY}\n- **Test**: Fix failing tests"; fi
          if [ "${{ needs.coverage.result }}" == "failure" ]; then BODY="${BODY}\n- **Coverage**: Add tests"; fi
          if [ "${{ needs.package.result }}" == "failure" ]; then BODY="${BODY}\n- **Package**: Fix metadata"; fi
          if [ "${{ needs.audit.result }}" == "failure" ]; then BODY="${BODY}\n- **Security**: Update dependency"; fi
          if [ "${{ needs.quality.result }}" == "failure" ]; then BODY="${BODY}\n- **Quality**: Fix issues"; fi
          BODY="${BODY}\n\n*Auto-generated*"
          echo -e "$BODY" | gh pr comment ${{ github.event.pull_request.number }} -F - --repo ${{ github.repository }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}